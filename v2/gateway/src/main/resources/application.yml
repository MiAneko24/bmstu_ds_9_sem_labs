server:
  servlet:
    context-path: /api/v1
    application-display-name: gateway
  forward-headers-strategy: framework
spring:
  servlet:
    multipart:
      resolve-lazily: true
#custom:
#  services:
#    loyalty: /
