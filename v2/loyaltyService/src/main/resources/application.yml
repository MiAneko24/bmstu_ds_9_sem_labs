server:
  servlet:
    context-path: /api/v1
    application-display-name: loyalty_service
  forward-headers-strategy: framework
spring:
  servlet:
    multipart:
      resolve-lazily: true
  datasource:
    url: jdbc:postgresql://localhost:5432/loyalties
    username: loyalty
    password: loyalty
    driver-class-name: org.postgresql.Driver
